// deploy/05_pair.ts

import { Deploy } from '@zoolabs/contracts/utils/deploy'

export default Deploy('UniswapV2Pair', [], async({ getChainId, hre, deploy, deployments, deps }) => {
  await deploy([])
})
