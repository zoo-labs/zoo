extends layout

block content
  script.
    if(localStorage.czUser) {
      window.location.href = '/feed'
    }
  main#HomePage
    #HomePage-inner
      img(src="/images/logo.jpg", alt="CryptoZoo")
      a#MetamaskLogin.button(onclick="metamaskLogin()") Login with Metamask
