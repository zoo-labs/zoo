import React, { useRef, useState } from "react";

const Web3Connect = ({}: any) => {
  return (
    <div className="flex flex-col items-center justify-center w-full h-full ">
      Connect
    </div>
  );
};

export default Web3Connect;
