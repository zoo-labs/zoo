{
    "name": "@zoolabs/graphnode",
    "version": "1.0.0",
    "license": "MIT",
    "private": true,
    "scripts": {
      "start": "yarn clean && cd ./docker && docker-compose up",
      "clean": "cd ./docker && docker-compose down && rm -rf ./data && docker image prune -f"
    }
  }